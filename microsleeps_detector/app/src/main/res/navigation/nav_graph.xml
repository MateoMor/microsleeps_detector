<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/Dashboard">

    <fragment
        android:id="@+id/Dashboard"
        android:name="com.example.microsleeps_detector.Dashboard"
        android:label="@string/dashboard"
        tools:layout="@layout/dashboard">
        <action
            android:id="@+id/action_dashboard_to_camera"
            app:destination="@id/CameraFragment" />
    </fragment>

    <fragment
        android:id="@+id/CameraFragment"
        android:name="com.example.microsleeps_detector.CameraFragment"
        android:label="Cámara"
        tools:layout="@layout/fragment_camera" />

</navigation>